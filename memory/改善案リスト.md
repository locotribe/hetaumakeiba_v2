# hetaumakeiba_v2 改善案リスト

## 🔴 最優先（クラッシュ後に発覚・要確認）

### 1. v13.40.4〜v13.41.1で新設したファイルが一度もコミットされていない
- 2026-06-12、Android Studio/CLIの再インストール後に現状把握のため再調査した結果、
  以下7ファイルがgit管理外（未追跡）であることが判明した。
  各コミット（4b2b3fd/c1353bb/e9b0dc6）は「これらを参照する既存ファイル側の変更」のみを
  含んでおり、新規作成したファイル自体は`git add`されていなかった。
  - `lib/services/user_session.dart`（13行・v13.40.4 UserSession本体）
  - `lib/view_models/race_result_view_model.dart`（519行・v13.41.0）
  - `lib/view_models/race_schedule_view_model.dart`（370行・v13.41.0）
  - `lib/models/course_diagram_model.dart`（175行）
  - `lib/services/course_diagram_service.dart`（105行）
  - `lib/widgets/shutuba_tabs/course_diagram_painter.dart`（145行）
  - `lib/logic/race_info_parser.dart`（38行・v13.41.1）
- 幸いディスク上には実体が残っており内容も完全（今回のクラッシュで消失はしていない）。
  現状の作業ツリーではビルドは通るはずだが、このまま別環境へクローンすると
  上記クラスが見つからずコンパイルエラーになる。
- 次回対応: 上記7ファイルを、現在ステージ済みのコース平面図関連の変更
  （座標JSON24点・コース画像GIF10点・memory/配下）および未ステージの変更
  （`.gitignore`/`pubspec.yaml`/`pubspec.lock`/`race_info_tab.dart`）とまとめてコミットする。

## 🔴 高優先度（進行中）

### 2. コース平面図の座標JSON再生成（edge_points/approach_paths）－ 東京芝のみ完了、残23件は明日対応
- 2026-06-12中に**Python側の解析アルゴリズム修正に成功（v.1.65、ダイクストラ法）**。
  ラスタースキャン順ではなく「index0=ゴール・配列順=逆走一筆書きパス」の
  `edge_points`を生成できるようになった。
- 修正版`tokyo_shiba_base_coords.json`を配置・実機確認済み:
  - `edge_points[0] = [400, 270]`（ゴール位置）、累積距離2083.1m =
    `base_lap_distance`(2083.1m)と完全一致（比率1.000）、全区間1.4px以下の滑らかなパス
  - 実機でGマーカー・走行軌跡が画像内の正しい位置に描画されることを確認。
    Sマーカーは画像と比べて若干ズレているが画像内には正しく表示されている
    （`approach_paths`未生成のため。下記参照）
- **残り23ファイル（tokyo_dirt含む）は旧データ（ラスタースキャン順、
  累積距離比145〜235倍）のまま未修正**。次回セッションでv.1.65アルゴリズムを
  適用して再生成する。
- `course_diagram_painter.dart`（145行）は元々「本来ロジック」
  （`approachPathFor`/`indexAtDistance`/`positionAtDistance`/`nearestEdgeIndex`を
  使う分岐、暫定スキップなし）のままだったことを確定（コード変更不要）。
  走行軌跡の色のみ視認性向上のため`Colors.amber`→`Colors.red`に変更済み。
- `race_info_tab.dart`の`_buildCourseDiagramSection()`によるUI統合は完成済み。
- 次回対応（詳細は[[work-20260612]]参照）:
  1. 東京芝のSマーカー位置ズレについて追加報告があれば確認
  2. v.1.65アルゴリズムで残り23コース分の座標JSONを再生成・配置
  3. シュート（引き込み線）対応: Python側で`approach_paths`の生成手法を設計・実装し、
     Flutter側の`approachPathFor != null`分岐への描画統合に着手

### 3. 高低差グラフの消滅（出馬表詳細ページ）－ 案B残課題
- 案A（共通パーサー`lib/logic/race_info_parser.dart`の実装・適用）は内容を再確認し、
  現存・実装一致を確認済み。`race_page.dart`/`shutuba_table_page.dart`系の
  `_createPredictionDataFromRaceResult()`が`raceResult.raceInfo`から
  `trackType`/`distanceValue`/`direction`/`courseInOut`を復元して
  `PredictionRaceData`に渡す実装になっている。
- 次回対応: 案B（コース図＋高低差グラフの統合表示）に着手。詳細は[[work-20260611]]参照。
- `lib/widgets/shutuba_tabs/race_info_tab.dart`の`_buildElevationChartSection()`冒頭に
  調査用`debugPrint('[Debug] raceId=...')`が残置されている（現在も存在を確認）。
  動作確認後に削除すること。

## 🟠 中優先度

### 4. .bakファイルの整理
- 2026-06-12時点で以下10個の`.bak`/`.bak2`〜`.bak4`ファイルが
  リポジトリ内に未追跡のまま散在している。不要なら削除、
  参考用に残すならリポジトリ外（別フォルダ）への移動を検討。
  - lib/logic/hit_checker.dart.bak
  - lib/logic/parse.dart.bak
  - lib/logic/ticket_aggregator.dart.bak / .bak2
  - lib/screens/race_statistics_page.dart.bak
  - lib/screens/saved_tickets_list_page.dart.bak 〜 .bak4
  - lib/widgets/purchase_details_card.dart.bak

### 5. web/・windows/・firebase.json の扱い
- クラッシュ後の再インストールで`flutter create .`相当の処理が走ったとみられ、
  `web/`・`windows/`ディレクトリと`firebase.json`が新規に出現している（git未追跡）。
  android/iosは引き続き追跡対象。Web/Windows対応を有効化するかどうか方針を決め、
  追跡対象に含めるか`.gitignore`に追加するかを判断する。

## 🟡 低優先度

### print()削除
- ~~114箇所でprint()が使用されていた~~
- ~~debugPrint()に置き換え済み（v13.40.1）~~

### constウィジェット化
- 約594箇所でconst化が可能
- flutter_lintsのprefer_const_constructorsを有効化

### エラー・空状態のUI追加
- 多くのFutureが失敗すると空のリストを返す
- ErrorWidgetまたはリトライボタン付きエラー表示が必要

### スケルトン表示の追加
- 全画面でCircularProgressIndicatorのみ使用
- シマー/スケルトンUIにすると体感速度が改善

## 完了済み修正（参考）
- v13.39.0: bettingDict一元化・券種別集計テーブル追加
- v13.39.1: 応援馬券統合表示・オーバーフロー修正
- v13.39.2: 複数券種混在馬券の解析バグ修正
- v13.39.3: ボックス・フォーメーション購入金額修正
- v13.40.0: 払戻・的中率・回収率の表示追加
- v13.40.1: print() → debugPrint() 一括置換
- v13.40.2: 馬券UIピクセル微調整（フォント・ボックス幅・式別エリア）
- v13.40.3: DBマイグレーションのv3→v4抜け修正・全catchブロックにrethrow追加
- v13.40.4: localUserIdグローバル変数を廃止しUserSessionサービスへ移行（17ファイル）
  ※ロジック変更はコミット済みだが本体`lib/services/user_session.dart`は未コミット
  （上記🔴最優先「1.」参照）
- v13.40.5: volatility_analyzer.dartの8解析をcompute()でオフロードしUIフリーズを防止
- v13.40.6: タブレットNavigationRailを5項目に拡張し重賞一覧の到達不可バグを修正
- v13.41.0: ViewModel分離完了（race_result_page.dart→race_result_view_model.dart、
  race_schedule_page.dart→race_schedule_view_model.dart）
  ※ロジック変更はコミット済みだがViewModel本体2ファイルは未コミット
  （上記🔴最優先「1.」参照）
- v13.41.1: 高低差グラフ消滅バグの案A対応。共通パーサー`race_info_parser.dart`を新設し、
  `race_page.dart`/`shutuba_table_page.dart`の`_createPredictionDataFromRaceResult()`に適用。
  ※`race_info_parser.dart`本体は未コミット（上記🔴最優先「1.」参照）

## 現在のバージョン
13.41.1+1
