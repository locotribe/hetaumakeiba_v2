name: hetaumakeiba_v2
description: "競馬データ管理アプリ"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  mobile_scanner: ^3.5.0
  sqflite: ^2.3.0
  path_provider: ^2.1.1
  http: ^1.2.0
  html: ^0.15.0
  url_launcher: ^6.3.0  # 追加
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true