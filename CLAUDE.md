# CLAUDE.md

## Release Workflow
- `pubspec.yaml`のバージョン更新を指示された場合、完全なCalVer形式（YYYY.M.D+YYMMDDXX）を採用すること。
- 同じ日に複数回更新する場合は末尾のXXをインクリメントすること。
- リリース作業を行う際は、必ず「1. ファイル更新とdiffの提示」と「2. 人間の許可を得た後でのコミット・タグ付け・プッシュ」の2段階に分けること。

## Memory Management (project_overview.md)
- 作業終了時に `project_overview.md` を更新する際は、本日の作業内容を履歴の先頭に追加すること。
- 履歴が長くなりすぎないよう、直近7日分（または直近5件）のみを残し、古い履歴は自動的に削除すること。
- 新たな未解決バグや次回持ち越しの課題がある場合は、履歴の中ではなく、ファイルの先頭付近に設けた「未解決課題 (Current Issues)」セクションに更新・追記すること。解決した課題は同セクションから削除すること。

## Coding Rules & Constraints
- AIの個人的な判断による仕様変更、UIデザインの変更、および既存ロジック・ライブラリのリファクタリングを固く禁じる。
- コードを出力する際、`// ...existing code` のような省略は一切行わないこと。要求されたスコープの、完全でそのままコピー＆ペースト可能なコードブロックを必ず提供すること。
- 一時的なコメントを追加する場合は、必ず以下の日本語プレフィックスのいずれかを使用すること: `// [修正]`, `// [追加]`, `// [削除]`, `// [一時]` （フォーマット例: `// [修正] 内容 (v.バージョン番号)`）
- 異なる/古いバージョンの古い一時コメントがコードに含まれている場合はクリーンアップ（削除）し、現在作業中の一時コメントのみを含めること。
- `//` または `///` で記述された標準的な永続コメントは絶対に削除せず、常に保護すること。
